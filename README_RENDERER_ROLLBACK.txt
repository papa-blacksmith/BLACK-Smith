BLACK Smith Renderer Rollback Fix

今回の目的:
武器・制御点を確実にキャンバスへ表示します。

変更:
- 複雑な全パーツ重ね描画を完全に停止
- 選択中パーツを正常実績のあるdrawEditorで直接描画
- 起動時と表示リセット時にviewBoxを0 0 1000 360へ強制復元
- パーツ選択、保存、ロック、表示設定、トランスフォーム値は保持

一時停止:
- 全パーツ同時表示
- 位置・回転・拡縮のキャンバス反映

表示の安定確認後、別方式で段階的に戻します。
