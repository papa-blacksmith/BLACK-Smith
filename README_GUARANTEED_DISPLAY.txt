BLACK Smith Guaranteed Display Fix

目的:
選択中のパーツを必ずキャンバスへ表示する安定版です。

修正:
- 複雑な全パーツ重ね描画を一時停止
- 選択中パーツを既存の安定したdrawEditorで描画
- パーツ位置・回転・拡縮は維持
- 座標・倍率を安全範囲へ補正
- 起動時に100%・中央表示へ強制リセット
- 表示リセット時に再描画
- SVGのvisibilityとopacityを強制

この版で表示を安定させてから、
全パーツ同時表示を安全な別レイヤー方式で再実装します。
